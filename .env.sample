# XDebug Configuration
# See DEVELOPER_GUIDE.md for detailed documentation

# IDE Key - use PHPSTORM for both VSCode and PhpStorm compatibility
XDEBUG_IDEKEY=PHPSTORM

# XDebug configuration options
# mode=req: request mode (recommended for both IDEs)
# idekey: must match the IDE key setting in your debugger
XDEBUG_CONFIG=idekey=PHPSTORM mode=req

# PHP IDE Configuration
# serverName must match the server configured in your IDE
PHP_IDE_CONFIG=serverName=localhost
